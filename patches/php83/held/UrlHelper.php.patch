--- a/vendor/symfony/helper/UrlHelper.php
+++ b/vendor/symfony/helper/UrlHelper.php
@@ -415,7 +415,7 @@
 
   for ($i = 0; $i < strlen($text); $i++)
   {
-    $char = $text{$i};
+    $char = $text[$i];
     $r = rand(0, 100);
 
     # roughly 10% raw, 45% hex, 45% dec
