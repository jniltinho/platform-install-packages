--- a/vendor/symfony/vendor/creole/util/sql/SQLStatementExtractor.php
+++ b/vendor/symfony/vendor/creole/util/sql/SQLStatementExtractor.php
@@ -143,7 +143,7 @@
             trigger_error("substring(), Endindex out of bounds must be $startpos<n<".($len-1), E_USER_ERROR);
         }
         if ($startpos === $endpos) {
-            return (string) $string{$startpos};
+            return (string) $string[$startpos];
         } else {
             $len = $endpos-$startpos;
         }
