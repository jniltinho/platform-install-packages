--- a/vendor/symfony/i18n/sfNumberFormat.class.php
+++ b/vendor/symfony/i18n/sfNumberFormat.class.php
@@ -192,7 +192,7 @@
       // now for the integer groupings
       for ($i = 0; $i < $len; $i++)
       {
-        $char = $string{$len - $i - 1};
+        $char = $string[$len - $i - 1];
 
         if ($multiGroup && $count == 0)
         {
