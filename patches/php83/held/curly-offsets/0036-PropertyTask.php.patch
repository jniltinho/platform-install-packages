--- a/vendor/symfony/vendor/phing/tasks/system/PropertyTask.php
+++ b/vendor/symfony/vendor/phing/tasks/system/PropertyTask.php
@@ -412,7 +412,7 @@
             if ($pos === (strlen($value) - 1)) {
                 array_push($fragments, '$');
                 $prev = $pos + 1;
-            } elseif ($value{$pos+1} !== '{' ) {
+            } elseif ($value[$pos+1] !== '{' ) {
 
                 // the string positions were changed to value-1 to correct
                 // a fatal error coming from function substring()
