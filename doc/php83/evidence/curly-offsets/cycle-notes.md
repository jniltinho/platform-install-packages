# Independent-review interpretation

- Claude's first execution independently reproduced all 204 three-class behavior rows; its report and Cursor's review refer to the unchanged behavior tools. The original whole-directory freeze also lists unrelated purity-builder files subsequently hardened; `primary-frozen-tools-r2.sha256` records that deliberate new phase, not silent mutation of the behavior run.
- Cursor executed eight local collector tests and shell parsing, not the VM. Its review correctly limits runtime coverage to three files and selected methods. Original PHP 8.3 negative compilation is supplied by the separate 43-file compiler controls, not by the behavior runner.
- The behavior collector validates expected diagnostic category, severity, source basename and line, and exact same-runtime row equality. Cross-runtime message hashes are retained but not golden-pinned: full message-text equivalence is not claimed. The `php83_diagnostic_severity_change_explicit` flag documents the predeclared mapping, not a separately computed diff; validated native overread rows make that mapping observable for each class.
- Google multibyte length overreads and incorrect returned lengths are retained baseline defects, not repaired by a syntax-only batch. PHP 8.3 warning severity can affect application error handlers; those integration effects remain untested.
- Runtime binaries identify isolated labs, not newly built production packages. Line pins are intentionally tied to immutable source identities; changed source needs new review.
- Grok's bounded attempt ended with exit 124 and no output. It is not a passing execution. The exact available free OpenCode model is `opencode/muse-spark-1.3-contributor-free`; its separate execution/review is tracked independently.

- Cursor's later separately authorized `cursor-compiler74` phase independently executes the entire 43-file PHP7.4 compiler/tokenizer matrix; the earlier no-VM statement applies only to its first behavior-harness review. All86 compiler and43 token results match the primary report byte-for-byte.
- Cursor's final coverage sentence says “9×204”; that is a wording error in the retained advisory report. The actual behavior denominator is **204 total case rows across nine processes**, as its earlier table, raw reports and comparison verify.
- Full staged `git diff --check` reports whitespace in exact upstream-context patch files and two retained failed-attempt stderr captures. Those bytes are intentionally preserved (changing them would invalidate source/patch/evidence hashes). The check excluding these exact artifact paths passes; new harness/documentation files are not exempted.
