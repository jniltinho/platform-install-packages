Read-only bounded independent local review, no VM/network commands, no edits, no git, no collector/build main, no external source. Read tools/php83/template-generation/collect.py and test_collect.py, and optionally build.py/test_build.py/run.sh/probe.php. Actually run PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tools/php83/template-generation -p 'test_*.py' -v (expected27), plus bash -n tools/php83/template-generation/run.sh. Review two narrow fixes: persist now uses unique sibling mkstemp+finally cleanup so old .progress.tmp cannot block; strict parse_checksums rejects malformed/duplicate paths with ValueError. Prior collector retained at doc/php83/evidence/template-generation/cursor-reviewed-r2-collect.py for comparison. Verify new tests cover stale temp, serialization failure, replacement failure, valid and malformed/duplicate checksums; use additional safe mock/tempdir adversarial tests if useful, never call remote/identities or main. Report actual test count/exit, defects, limitations. No runtime/compatibility approval; collector intentionally nonacceptance. Stop denied operations rather than bypass permissions. Public results only, no chain-of-thought.
