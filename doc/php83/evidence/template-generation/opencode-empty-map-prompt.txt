Read-only local independent review of narrow collector-only fix; no VM/SSH/network/source edits, no build/collector main, no external source reads. Read tools/php83/template-generation/{collect.py,test_collect.py}, compare old doc/php83/evidence/template-generation/generation-reviewed-r3-collect.py. Required actual tests: separately run PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tools/php83/template-generation -p test_build.py -v (10) and same -p test_collect.py (19). Do not run test_method.py: it validates historical reports against previous execution-harness hashes; these frozen old reports remain historical, not currentcollector execution. Run bash -n tools/php83/template-generation/run.sh. Verify only decode_outputs change accepts exactly JSON empty list[] as empty PHP map, still accepts dict, rejects nonempty list/scalars/null, with no broad source/probe changes. Review failure report evidence/template-generation/generation-primary.json terminal37rows as actual context if useful. No golden approval or compatibility claims. Report counts/exits and any defects; stop denied operations, no bypass. Public results only no thought text.
