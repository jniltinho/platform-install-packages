Coordinator authorized exclusive BOTH baseline74/native83 for four-process Criteria marker repeat; previous owner released. This is actual runtime repeat using existing frozen root-owned stages ONLY. Read tools/php83/criteria-marker/repeat.py,collect.py,compare.py and existing primary-result.json. Execute exactly `python3 tools/php83/criteria-marker/repeat.py`, preserving stdout/stderr/exit into doc/php83/evidence/criteria-marker/claude-repeat-command.*. Helper uses already approved SSH configs and existing stages; no staging/build/source change, SQL or production. It verifies parent-pinned sources/runtime before/after and exact primary equivalence of rawstdout/stderr/decodedrecords. Never invoke collect.py main (would stage). On permission or runtime failure stop and report; no alternate access or retry. Do not select null declaration despite functional parity: representation deltas explicitly remain. Public final concise observed4process results and any findings; no hidden reasoning. Write only criteria-marker/claude* evidence, no source edits. Bound150sec within180sec CLI timeout.
