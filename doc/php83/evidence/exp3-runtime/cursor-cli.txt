Vou ler o AGENTS.md e revisar os scripts do harness; em seguida executo só o comando lab aprovado e reporto os resultados observados.Scripts revisados; preparo o diretório de evidência e executo o comando lab aprovado (limite ~200s).Batch concluiu com exit 0; vou extrair contagens, falhas e diagnósticos do `cursor.json`.## Execução (Cursor)

| Campo | Valor |
|--------|--------|
| Comando | `ssh … php83 python3 …/batch.py` → `cursor.json` / `cursor.stderr` / `cursor.exit` |
| Exit do wrapper | **0** (`cursor.exit`) |
| stderr | **0 bytes** |
| Host | `kaltura-php83-lab` |
| Rows | **24** (original/exp3 × standard/minimal × 6 cases) |
| Pass / fail | **18 / 6** |
| `verified_extracted_files` | **15183** |
| `zip_sha256` | `1c64edb5…c985e3` (bate com o assert do harness) |
| `application_acceptance` | **false** |

## Falhas (6)

| source | ini | case | exit | diagnóstico |
|--------|-----|------|------|-------------|
| original | standard/minimal | legacy-json | 255 | curly braces em `Services_JSON.class.php:176` |
| original | standard/minimal | zend-json | 255 | curly braces em `Zend/Json/Encoder.php:557` |
| original | minimal | doc-comment-consumer | 255 | `API fixture requires the unprivileged audit runner` |
| exp3 | minimal | doc-comment-consumer | 255 | mesmo `api-bootstrap.php:15` |

Os 4 fails de JSON no **original** são controles esperados em PHP 8.3. Em **exp3**, legacy/zend-json passam (standard e minimal), com deprecations de dynamic property.

## Harness (só revisão; sem edições)

- `batch.py`: revalida ZIP SHA + cada arquivo extraído vs archive; 24 rows; `timeout=70`; imprime JSON; `application_acceptance=False`.
- `run-one.sh`: `nobody`/`nogroup`, `PrivateNetwork`, filtro de socket, RO bind do source, overlay de cache só em consumer; `RuntimeMaxSec=60`; preserva exit do PHP.

## Escopo / gates

- Evidência desta execução: só `doc/php83/evidence/exp3-runtime/cursor.*`.
- Sem edits de app/package/deploy; sem comandos extras na VM.
- Passar o harness **não** é aceitação da aplicação.
- Esta execução Cursor **não** substitui review independente (Claude/Grok/`agent` conforme `AGENTS.md`).
