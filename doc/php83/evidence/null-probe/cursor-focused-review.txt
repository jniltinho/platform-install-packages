Vou revisar os quatro artefatos do null-probe e cruzar com as evidências JSON, com resumo Python limitado (sem dumps grandes).Agora cruzo as quatro evidências com um resumo Python limitado: contagens, hashes, oráculos e cleanup.Confirmando o terceiro oráculo absoluto no fixture e se `RuntimeException` de asserção nunca vaza como erro normalizado.**Veredito:** evidências Codex/Claude batem no harness local e passam os contratos do null-probe; isto não é aceitação de aplicação.

Codex e Claude batem bit-a-bit nos quatro runs de classe (41 casos cada; 10×4 ops + `legacy-namespace`) e nos quatro de SQL (10 casos `EXPECTED`, com `missing`/`transitive`/`wrong-partner` → `[]`), com `functional_checks_passed=True`, `application_acceptance=False`, hashes de `probe.php`/`run.sh`/`dependencies.php`/`run-dependencies.sh`/`api-bootstrap.php` e dos collectors iguais ao checkout, e identidade de artefato exp4/exp5 (`a1ce…`/`ef4d…`, 15184/15189). Paridade é só same-runtime (`exp4==exp5` em 74 e 83); 74≠83 (2 `Error` vs 7 `TypeError`) e não é oráculo absoluto por caso. Em `probe.php`, `RuntimeException` de asserção é re-lançada e nas evidências não há `RuntimeException` em linhas `error` — só `Error`/`TypeError` de entrada. Oráculos absolutos de classe são três: `partner/null` → `ALWAYS_ALLOWED_ACTIONS`, `partner/dummy` → `DUMMY`, e a asserção serializada `legacy-namespace` (`plain`/`ns`); dizer “só dois” subconta. Dependências: DB owned parou (`stopped:True`, units distintas); stdout sanitizado para `EXPECTED` e stderr só `stderr_sha256`+diagnostics, enquanto o collector de classe persiste `bootstrap_stderr` cru (vazio em 74; deprecações libxml em 83, sem KS — fixture usa `$ks=''`). Gap: digests de stderr SQL diferem entre executors (unidades distintas), então não são comparáveis cross-run; limite: diagnóstico de bootstrap/PDO retido, escopo harness-only, sem aprovação full-app.
