Vou executar o verificador exato e, em paralelo, localizar o `verification.json` do Codex e os arquivos de evidência para comparar as lacunas.Confirmando identidade byte a byte do relatório e inspecionando o que a paridade/API deixam de fora.Verificador Cursor gravado em `/tmp/php83-exp6/cursor-verification.json` — idêntico ao Codex (`doc/php83/evidence/exp6-candidate/verification.json`: mesmo SHA do JSON e `zip_sha256` `da11cbb9…`, builds idênticos, sem entradas faltantes). Fontes não foram alteradas; sem chamadas a VM.

`comparison.json` confirma `independent_equal` em todas as linhas e `new_or_changed_groups: []`, com remoção dos diagnósticos de `KalturaActionReflector` (exp5→exp6), mas **`application_acceptance: false`**: no 83 o exp6 ainda deixa **33 grupos / 847 eventos** de diagnóstico residual (vs 1/6 no original74). `parity.json` tem paridade tipada nos casos cobertos (json/doc-comment) e 24 linhas candidatas com exit 0, porém também **`application_acceptance: false`** — harness CLI focado ≠ aceitação da aplicação. Linha CLI do exp5 é **lacuna de cobertura**, não prova de bug; a baseline funcional **original74** continua obrigatória. Aceitação AIO/FPM/média/distro e demais gates do manifesto seguem abertos.
