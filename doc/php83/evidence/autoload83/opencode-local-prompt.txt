Perform a bounded independent READ-ONLY repository-only validation. Do not edit any files, run git, contact VMs, run network commands, read any path outside the current repository, or launch other agents. Prior external source read permission was denied; do NOT retry denied paths via any tool, shell, copy, symlink, or workaround. This is a separately scoped safe task, not completion of that external-source review.

Read tools/php83/base-object-ternary/test_ternary.py, collect.py, build.py and local patches/php83/held/base-object-ternary/{manifest.json,base-object-ternary.patch}. Do not run build.py or collect.py main (collector main contacts a VM). The unittest module has two tests that read external immutable source; do NOT execute test_two_bytes_only or test_source_drift. Execute only the following eight local evidence/collector tests using a single bash command, with PYTHONDONTWRITEBYTECODE=1 and PYTHONPATH=tools/php83/base-object-ternary:
python3 -m unittest -v test_ternary.Proof.test_actual_record_validation test_ternary.Proof.test_timeout test_ternary.Proof.test_boolean_exit test_ternary.Proof.test_wrong_association test_ternary.Proof.test_extra_diagnostic test_ternary.Proof.test_source_hash test_ternary.Proof.test_missing_case test_ternary.Proof.test_side_effect

Then review the local two-parentheses patch, the manifest's patch SHA256 against local patch bytes, and collector fail-closed behavior. You may use shell sha256sum ONLY on explicitly repository-local paths. Report actual command exit and counts, review findings and limitations. Two external-input tests are NOT_EXECUTED in this task. No artifact promotion, whole application acceptance, or independent runtime execution claims. If any required tool permission fails, stop that operation and report BLOCKED; do not bypass safeguards. Keep response concise. This is a review of named files, not broad structural discovery.
