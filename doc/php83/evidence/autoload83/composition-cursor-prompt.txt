You are the actual Cursor independent reviewer/executor. Workdir /home/nilton/Projetos/nilton/NOVOS/platform-install-packages-php83. Read AGENTS.md. Other agents are active. Do not edit tools, patches, source, prior evidence, git or any VM. No SSH, runtime PHP execution, SQL, network/backend calls, service/package/release work. Only write doc/php83/evidence/autoload83/composition-cursor-* evidence. Do not invoke collect.py main; only local Python imports/validation and unittests. Do not spawn other agents. Bounded 240-second local task.

Frozen hashes: composition-harness-final.json; includes current composition tools and base collector hardening. Actually run both commands, capture separate stdout/stderr/exit under your prefix:
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tools/php83/autoload83-composition -p 'test_*.py' -v
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tools/php83/autoload83 -p 'test_*.py' -v
Expected 13 composition and 21 base tests, but report actual, not assumed.

Independently replay retained reports locally by importing the corresponding collect.py under a unique module name via importlib. Never call main/inventory/SSH. For base primary-r3.json and claude-primary.json call validate(result,case,runtime,variant,exit,stderr,report.identity.hashes,stdout) on each record. Expect20PASS+3EXPECTED_FATAL+3 actual ValueError for cli-tasks specifically 'Real entrypoint failed (not waived)'. They are real empty-project failures, overall originalreportFAIL, NOT26passes. New configured-task cases really load37 sourcefiles and list same tasks, tree1630files.

For composition-primary-r2.json, derive per-record hash whitelist from source_identity.hashes: variant/path => /audit/source/path; fixtures/path => /audit/fixtures/path; composition harness probe.php => /audit/probe.php; own fixtures/path => /audit/composition-fixtures/path. Call composition validate(result,case,runtime,variant,exit,stderr,whitelist). Expected27PASS+3EXPECTED_FATAL/30. Record input report hashes, perrecord status and stderr/exit of replay under your prefix. This is retained evidence replay, NOT new runtime execution.

Review actual frozen probe/runner/collectors adversarially. Focus: positive exact row/queue/hit/errortrace, real loaded target+winner identity, intentional native74 implicit __autoload removal on first SPL registration vs candidate persistent closure; shadow vs actual winner; thrown exceptions propagate and no subsequent handler; repeated fullCLI include flushes pre-fatal provenance while exactnative stderr/exit verifies original function redeclaration107 vs candidateclass36; no false idempotence/fullparity; source/harness readonly identity and socketdenial; exact diagnostics SHA/category/severity/source/line and native fatal matching. Explain limitations honestly. If useful run additional mutations in memory without modifying tools. Do not waive a bug because current tests pass. Prior independentreviews missed loaded=[] collector bug already preserved, so do not infer runtime correctness from local tests.

Return public concise report with actual commands/tests/exits, exact3report replay counts, concrete findings/lines and limitations. No wholeapplication/release approval. Save review under composition-cursor-review.json or .txt. No shared files touched. Verify frozen input hashes after all activities; hashdrift or permission failure is notsuccess.
