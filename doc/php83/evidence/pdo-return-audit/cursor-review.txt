Vou rodar os unittests locais e cruzar com o contrato original de PropelPDO/KalturaStatement no Rigel 18.20.0.Lendo os métodos originais e a evidência de contratos nativos PDO.**Local:** `python3 -m unittest discover -s tools/php83 -p "test_*.py"` → **136 OK / 0.080s**; isso só valida harness/coletores, não SQL real nem aceite da aplicação.

**Contratos (Rigel 18.20.0 + `native-83.json`):** `PDO::setAttribute` e `PDOStatement::execute` têm retorno tentativo `bool`. `PropelPDO::setAttribute` (L206–214) no branch `PROPEL_ATTR_CACHE_PREPARES` não retorna nada e no `default` chama `parent::setAttribute` **sem** `return` — o bool some. `KalturaStatement::execute` chama `parent::execute` (ou dry-run) e **nunca** retorna; `bindValue` ainda devolve o bool do parent. Contraste: `DebugPDOStatement::execute`/`bindValue` preservam o retorno; `KalturaPDO` herda o `setAttribute` quebrado, mas `beginTransaction`/`exec`/`query` já devolvem o valor do parent.

**Não** adicionar `: bool` nem `ReturnTypeWillChange` às cegas: o aviso é sintoma de valor descartado; tipar sem devolver o resultado do parent muda o contrato público (hoje `null` implícito) ou mascara o problema.

**Próximos testes SQL mínimos:** (1) MySQL/`sqlite` via `KalturaPDO` — capturar retorno de `setAttribute(PROPEL_ATTR_*)` vs `ATTR_ERRMODE` e de `KalturaStatement::execute` em sucesso, falha com `ERRMODE_SILENT` e dry-run; (2) mesma matriz em `DebugPDO`/`DebugPDOStatement` como controle de paridade. Restaurar `return parent::…` (ou `return true` artificial) **altera** o comportamento público observado por chamadores como `PartnerActivity.php:104` (`$resultset = $statement->execute()`); só asserções de diagnóstico sem patch **não** mudam retorno.
