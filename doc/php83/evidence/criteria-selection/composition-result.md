# Cumulative Criteria native83 primary

**Bounded primary and actual Cursor independent native repeat PASS. No patch/artifact
selection or cache acceptance. The historical cross-engine layout FAIL remains.**

Current preparer independently reviewed by actual Claude: exit0, eight local
tests reported, no blocking source-preparer bug. The historical r1 evidence lacks
the later same-buffer preparer hash; new r2 records current hash, strict patch
command, base/patch/after hashes. Both preparation manifests are identical.

Actual Claude harness review timed out at180s (exit124, no public result), retained
without a pass claim. Actual OpenCode Muse Spark1.3 Free then completed exit0,
reported17 local tests and shell syntax PASS, and found no blocking harness logic
bug. These public summaries are not full provider tool transcripts; local test
logs independently corroborate the suite. Review caveats (exclusive ownership,
fresh stage, exact unchanged runtime, no overwrite of identities evidence) were
checked before primary execution.

Primary command `python3 tools/php83/criteria-composition/collect.py` completed
exit0. All four native83 processes exit0; parent-host fixture hashes, runtime,
modules, binary and linked-library hashes are unchanged before/after. Stage is
new, immutable and source-pinned; native83 ownership released after completion.
Baseline74 was not accessed.

- Exact19 real filter/hierarchy cases and serialized representations match.
- Exact16 return/alias rows and all six reflected return contracts match.
- Both intentionally invalid iterator controls retain native warnings, including
  the expected one-line source shift; exact native stderr is checked.
- Hierarchy dynamic deprecations: **24 → 0**; unrelated class negative control:
  **1 → 1**. No return-type diagnostics reappear.
- This is exp11 `e8333167…` versus cumulative `f8f72644…`, not only old original
  source. Removal of the new attribute restores every exp11 byte.

New post-observation adversarial local tests replay the retained primary without
VM access. They do not turn the primary into an independent native repeat and do
not prove a universal golden oracle: same tampering of both variants' uncodified
behavior is outside this bounded parity claim. Older original74/importedpayload
and strict cross-engine evidence is untouched; cache/backend policy remains open.

## Independent repeat checkpoint

Actual Cursor CLI completed exit0 and executed27 local tests plus the frozen
four-process native83 repeat. Helper exit0 and all stdout/stderr/body records,
source/runtime before/after identities and comparison are exactly primary.
composition-cursor.json and native sidecars retain actual observations;
cursor-repeat-command.log/.exit retain helper terminal. The provider public
summary is not a full tool transcript; native sidecars/report corroborate the
execution. Native83 released on terminal; baseline74 never touched.
