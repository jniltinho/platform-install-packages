{
  "exit": 0,
  "public": [
    "Independently reviewing the native83 composition gate \u2014 no edits, no execution beyond the bounded checks.",
    "Core harness mapped \u2014 now pulling the source probes and decision records.",
    "Local checks pass \u2014 now verifying pinned identities before the verdict.",
    "Independent review \u2014 criteria-composition readiness (no edits, no VM/SSH/tmp, no artifact build/selection).\n\nExecutor/reviewer: OpenCode + Muse Spark 1.3 Free (`opencode/muse-spark-1.3-contributor-free`), independent static review + bounded local checks only.\nEnvironment: repo checkout `/home/nilton/Projetos/nilton/NOVOS/platform-install-packages-php83`, host PHP not used for probes; native probes require `kaltura-php83-lab` + php8.3, not executed here.\nSource/patch/harness identities (pinned, not selected):\n- `prepare.py` sha `cceab42c\u20260e374` matches `composition-preparation-r2.json:prepare_sha256`.\n- UPSTREAM `58d534d0\u2026`, EXP11 `f2474f5b\u2026`, BEFORE `0626bb5b\u2026`, PRIOR `e8333167\u2026`; candidate-after `f8f72644\u2026`, patch `6c02aacf\u2026` per r2 manifest.\n- Current files match r2 fixture pins: `run.sh 8a05f180\u2026`, `verify.py 2bbea7fd\u2026`, `criteria-marker/attribute/probe.php 12800e23\u2026`, `criteria-return/probe.php 0d9fe571\u2026`.\n- Coordinator `coordinator-decision.md`: cumulative held experiment only (one `#[\\AllowDynamicProperties]` on Criteria hierarchy), no ZIP/manifest selection, strict cross-engine byte FAIL preserved, no cache/backend acceptance. `fixture.py`/`validate.py`/`prepare.py` encode the same (`FAIL_RETAINED`, `cache_acceptance False`, `patch_selected False`, `application_acceptance False`).\n\nScope reviewed: `fixture.py` (37 lines), `validate.py` (61), `collect.py` (69), `run.sh` (25), `prepare.py`, `verify.py`, `README.md`, `test_prepare.py`, `test_validate.py`, `criteria-marker/attribute/probe.php` (59), `criteria-return/probe.php` (88), `coordinator-decision.md`, `composition-preparation-r2.json`.\n\nActual test evidence (executed here):\n- `python3 -m unittest discover -s tools/php83/criteria-composition -p 'test_*.py' -v` \u2192 exit 0, `Ran 17 tests \u2026 OK` (10 prepare + 7 validate; includes exact-attribute-delta, drift/duplicate/shifted-anchor, symlink/duplicate-zip rejection, strict `--fuzz=0` patch replay, mode-ordering, native-format, line-normalization, target-severity).\n- `bash -n tools/php83/criteria-composition/run.sh` \u2192 exit 0.\n- `collect.py` NOT_EXECUTED (requires SSH `kaltura-php83-lab`, `/tmp/kaltura-php*-ssh.conf`, `/tmp/kaltura-php83-audit/*.zip`, sibling `*-artifacts/exp11/*.exp11.zip`, exclusive root-owned stage + sudo systemd-run; all out of authorized scope, correctly not attempted).\n\nReadiness claims checked:\n- Four native83-only processes: `prior/candidate \u00d7 filter/returns` via `validate.MODES`; `validate` enforces ordered modes, exit 0, `php startswith 8.3.`, criteria+filter identity binding, 19 filter cases (`marker.CASES`) + `legacy()` 7-representation inventory, 16 return rows + `CONTRACTS` (`getIterator:Traversable`, `rewind:void`, `valid:bool`, `key:mixed`, `current:mixed`, `next:void`) + `load_diagnostics==[]`, 2 invalid controls (`empty`,`exhausted`, values `[None,None]`, 2 diagnostics each, severity 2), exact reconstructed native stderr + normalized unrelated-diagnostic parity, hierarchy delta (prior has `Criteria::$creteria_filter_attached`+`myCriteria::$hint`+`KalturaCriteria::$creteria_filter_attached` deprecations; candidate none), unrelated `UnrelatedMarkerControl::$fixtureMarker` x1 severity 8192. Logic is fail-closed and consistent with probes. `mixed` signatures present \u2192 PHP74 correctly prohibited (validate hard-requires `8.3.`; README states 74 not run on exp11 mixed signatures).\n- Native diagnostic exactness: `native()` format + `return false` adaptation (fixture flips returns-probe handler `return true\u2192false`, rewrites 3\u00d7 `/audit/app/\u2192/audit/probe/` paths, injects `source_sha256+variant`) is coherent; exact stderr equality is unproven until native83 run.\n- Real identities: fixture pins both ZIPs + 4 dependency hashes via `source-pins.json`, verifies `EXP11` bytes before open, checks `identities.json` pin, `verify.py` enforces 11-file inventory + per-file hashes + no-symlink stage; `collect.py` re-verifies remote `sha256sum`, runtime (version+modules+extension_dir+ini+ldd-resolved binary/lib hashes incl. `json.so` for 74 path) equality against recorded attribute cycle before/after. Strict by design.\n- Sandbox: `run.sh` hostname gate, manifest-pin arg, pre+`trap EXIT` verify (drift \u2192 exit 70), `systemd-run` as `nobody:nogroup`, `PrivateNetwork=yes`, `socket/socketpair` denied, `ProtectSystem=strict`, `PrivateTmp`, `NoNewPrivileges`, read-only bind `$base:/audit/probe`, `MemoryMax=128M`, `RuntimeMaxSec=30`. Effects confined as claimed; live behavior unverified here.\n\nConcrete blocking items before any VM execution (no bypass):\n1. VM/SSH/`/tmp`/artifact prerequisites absent in this session \u2014 `collect.py` must stay NOT_EXECUTED until exclusive `kaltura-php83-lab` ownership with those paths is authorized.\n2. Remote `STAGE` must not pre-exist (`mkdir` without `-p` fails) \u2014 requires fresh or explicitly cleaned stage by lab owner, not forced.\n3. `runtime_before/after` must exactly equal recorded attribute-cycle `primary.json labs.83.runtime_before` (incl. ldd hashes) \u2014 any library rebuild blocks with `Runtime differs/changed`; re-baselining needs coordinator approval, not silent acceptance.\n4. `collect.py` overwrites `composition-identities83.json` without existence refusal (unlike `composition-primary.json`) \u2014-lab owner must archive prior evidence first.\n5. No local logic bug found in fixture/validate/prepare/run.sh static review that blocks VM scheduling; composition parity, cross-engine layout (FAIL retained), cache/application gates all remain OPEN \u2014 a local unit-test pass is not composition evidence.\n\nBounded verdict: LOCAL_HARNESS_PASS (17 unittest + `bash -n`), PREPARATION_IDENTITIES_MATCH_R2, COMPOSITION_PARITY_UNPROVEN (4\u00d7native83 observations + strict replay on lab still required). No patch selected, no cache/application acceptance, existing strict cross-engine FAIL retained."
  ],
  "raw_sha256": "dee60ee6b32d2544b3b27a5d722484a07464553ab2229d0909baad5bac3630e0"
}
