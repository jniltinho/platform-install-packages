# Criteria hierarchy: bounded cumulative experiment decision

Actual Cursor CLI completed with exit 0 and recommended `ALLOW_BOUNDED_EXPERIMENT`
after reviewing the existing primary/Claude/logical evidence. Its public report
records 23 locally executed tests, 22 checkpoint hash checks, explicit strict
comparison failure and typed logical-state comparison. The JSON CLI result is a
public final summary, not a full tool-call transcript; the detailed execution
claims are reviewer-reported. Original raw result SHA is retained separately.

Coordinator permits preparation of a **cumulative held experiment**, not selection
of a ZIP or production repair. One class-level AllowDynamicProperties attribute
explicitly preserves legacy dynamic behavior throughout Criteria's hierarchy,
including existing child `hint` writes and future dynamic members. This broader
exemption is intentional and cannot be called marker-specific suppression.

The declaration alternative is not selected because it changes representation
within each runtime. The attribute showed exact within-runtime19-case equality,
while three cross-engine subclass-order differences already exist without it.
Seven imported payloads retained typed logical values; that does not prove warm
cache compatibility. The original strict cross-engine byte FAIL is preserved.
No custom serialization, cache-key rewrite or cache/backend acceptance is implied.

`tools/php83/criteria-composition/prepare.py` verifies both original and exp11 ZIP
bytes and target identities, retains every prior null-alias/native-return byte,
and adds only the attribute to exp11 Criteria. The generated patch is cumulative
from original, so a future manifest must replace the single existing Criteria
entry, not append a duplicate target. Original/exp11 files are not modified.

Preparation at `/tmp/php83-criteria-composition-r1` yielded after SHA256
`f8f726443dbc3af363f6c08db2d6baa92591b37bb14abd104ac6a5951b5f6d8d`.
Eight local guard tests pass; exact original-to-candidate patch replay passes with
`--fuzz=0`, no offset. These are source-preparation results, **not native PHP
execution**. A later implementation adjustment reads archive bytes once, then
hashes and opens those same bytes; candidate/patch output is unchanged.

Next: independent preparer review; cumulative native alias/return/filter controls
versus exp11; then separately reviewed manifest/twice-built artifact and actual
artifact API/HTTP/trusted-TLS regression with exact diagnostic delta. Full cache,
application, three-distro packaging, release and cutover gates remain open.

Cursor's source list contains a filename typo (`runtime-next-repair-plan.md`);
the coordinator's original prompt correctly named `runtime-next-fix-plan.md`.
Do not treat that source-list entry alone as proof of reading the file.
