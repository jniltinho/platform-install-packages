# Failed experiment checkpoint

This checkpoint retains a rejected source experiment, not a compatible candidate.
Actual native74/83 primary processes and the actual Cursor native83 repeat prove
that renaming the local alias does not fix the reserved resolved parameter type.
The six preparation tests and independent Claude preparation review did not
claim PHP runtime acceptance; the later compiler failures supersede any repair
hypothesis. No new ZIP or migration selection is made.

The staged whitespace check flags only single-space blank context lines in the
hash-pinned unified patch. They are patch context syntax, not changed application
lines; preserve them exactly. The scoped check excluding that patch is clean.
Source, patch, stage, runner/probe and runtime evidence are retained separately.
