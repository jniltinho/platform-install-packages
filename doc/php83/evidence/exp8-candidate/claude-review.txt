Executei as três execuções em série, cada uma com seu laboratório isolado; não houve alteração de código, pacotes, commits nem produção. `collect.py` saiu com exit 0 e `functional_checks_passed: true`, e a unidade temporária de banco `php83-exp8-api-189e522b…` terminou `inactive`. Os lotes CLI também saíram com 0, tanto `claude-74` (12 linhas) quanto `claude-83` (36 linhas), com `.stderr` vazios.

- **API (4 linhas):** original74 = 0, original83 = 1 (o erro esperado de assinatura em `KalturaPDO::query()`), exp7 = 0 e exp8 = 0. As saídas exp7 e exp8 batem com a original74 e ficaram todas dentro da lista de saídas permitidas.
- **Comparação com o Codex, API:** tudo é igual, exceto `duration_ns` e `stderr_sha256`. A diferença de hash é esperada, porque o stderr bruto muda entre execuções e não é guardado.
- **Comparação com o Codex, CLI:** os dois relatórios são iguais linha a linha (sem contar a duração). O ZIP `67acaa1a…` e o exp7 anterior `a0934bbd…` foram conferidos, com 15191 arquivos cada.
- **Códigos de saída do CLI:** as 12 linhas da original74 saíram com 0. Na original83 houve 4 falhas fatais esperadas (exit 255), em legacy-json e zend-json nos modos standard e minimal. As 12 linhas de exp7 e as 12 de exp8 saíram com 0.
- **Diferença de diagnósticos exp7→exp8:** saíram exatamente 6 grupos de `vendor/propel/util/Criteria.php` (linhas 193 e 1259/1263/1267/1271/1275, 22 ocorrências cada, 132 no total) e nenhum grupo novo apareceu. Sobram 21 grupos no exp8.
- **Criteria cumulativo, não empilhado:** o patch `Criteria-native-returns.patch` parte do upstream original (`0626bb5b…`) e substitui o `Criteria-null-alias.patch`, que não está mais no exp8. O total continua em 14 patches.
- **Proteção contra alias nulo mantida:** o `Criteria.php` de exp7 e de exp8 difere só nas 6 assinaturas, e a verificação `$realtable === null ||` continua presente. Fora `Criteria.php` e o manifest, nenhum outro arquivo do ZIP mudou.
- **Artefatos só para 8.3:** os dois artefatos rejeitam execução em PHP 7.4 (`unsupported_cases`). A original74 continua sendo a referência de comportamento.
- **`test_exp8_api_collector.py`:** 10 de 10 testes passaram, todos locais, sem SSH nem banco. Eles cobrem a parada da unidade de banco quando a inicialização falha (três variantes), a lista de casos fixa, falha da original74 ou do candidato, o controle de assinatura, a saída não permitida que não é gravada e a falha na limpeza do banco.
- **Lacunas desses testes:** não cobrem recusa de sobrescrita, alteração dos arquivos de teste remotos (checagem de hash), falha do `verify-source.py` nem timeout no meio de `run-apache.sh`. Além disso, `stopped` aceita `unknown`, e `runtime_observations` é gravado do stderr sem filtro de campos (neste relatório só havia campos de versão e módulos).
- **Sem tokens nos logs:** nenhum token KS apareceu nas evidências; os únicos acertos da busca foram rótulos como `wrong-secret` e `tokenizer`. O Python rodou sem `-O`.

Limitações: nesta rodada o Claude executou e revisou; faltam a execução e a revisão pelo Grok (ou OpenCode como substituto) e pelo Cursor. Também não gerei `comparison.json`/`parity.json` com `compare.py` do Codex, porque ele grava arquivos e é tarefa do coordenador. Nada disso é aceitação da aplicação, porque AIO/FPM, cache, mídia e desempenho continuam em aberto.
