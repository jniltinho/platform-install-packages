Vou ler o AGENTS.md e o skill de codebase-memory, depois executar só o probe PHP 8.3 e a suíte local — sem editar fonte.Vou rodar o probe só no runtime 83 e a suíte local, e em seguida comparar com o `codex.json`.Probe concluiu com exit 0; agora comparo `cursor-83.json` com a fatia 83 do `codex.json` e valido a semântica pedida.**PASS (lab 83 only; sem aprovação de app).** `collect.py --runtime 83` → exit **0**; native **22** / metadata **24**; candidate warnings **0**; original metadata **42** `getClass()` deprecations; corpos iguais a `codex.json` (fatia 83).

Patch held: `getParameterClass` — `class|int`→classe; `class|class`/interseção/DNF→`null`; `SELF`/`PARENT` case-insensitive; unknown→`ReflectionException`; no-doc/reserved→erros; metadata serializada idêntica original↔candidate. Unittest local: **122** OK, exit **0**. Escopo: contrato reflection + metadata sintética; **sem** API SQL/HTTP, cache compartilhado nem aceite de aplicação completa. Executor Cursor (83); Claude detém 74; sem edits de fonte.
