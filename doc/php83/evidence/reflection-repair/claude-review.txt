1. Rodei o comando atribuído só no `baseline74`, por meio do `collect.py`. Não fiz nenhum outro SSH e não mexi no runtime 83, que é do Cursor. A saída foi exit `0`, e os arquivos ficaram em `doc/php83/evidence/reflection-repair/claude-74.{json,stdout,stderr,exit}`. O stderr veio vazio e o SHA-256 do JSON é `bdf60d2f…cbd2e0`.
2. Resultado: native com 15 casos e metadata real com 17. Nos dois, as linhas de native×candidate e de original×candidate batem exatamente. Isso inclui as linhas `error` e os `serialized_sha256`, e o `metadata.php` confirma que a segunda chamada serializa igual à primeira.
3. Comparei com as linhas runtime 74 do `codex.json`: os quatro `result` completos (`php`, `rows`, `warnings`) são idênticos. `base_artifact`, harness, bootstrap, `collector_sha256` e os hashes before/after do patch também batem.
4. O warning 8192 `Cannot use "parent" when current class scope has no parent` continua no 7.4 nas quatro variantes. Ele vem da declaração de `ReflectionProbeOrphan`, que existe só para <8.0, e fica mantido de propósito. O coletor só exige zero warnings no 8.3.
5. Revisão do patch retido: `getParameterClass` reproduz a semântica de `getClass()`. Retorna `null` para builtin, sem tipo, interseção/DNF e unions com mais de uma classe. Resolve `self` e `parent` com `strcasecmp`, lança a mesma `ReflectionException` para `parent` órfão e passa `$paramClass` para o ramo `allowsNull`.
6. O controle com falha foi mantido em `attempts/case-sensitive/`, com os quatro exits em `0`. Lá, o caso `upper` (`SELF`) virava `ReflectionException` "Class SELF does not exist" no 7.4 e no 8.3. Com o `strcasecmp` atual, os casos `upper` e `upperParent` batem com o nativo.
7. Os testes locais (`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tools/php83 -p 'test_*.py'`) deram 122 testes OK, exit `0`.
8. Não verificado: o cache de metadata só foi testado dentro de um processo, num `TemporaryFileSystem` privado. Cache compartilhado/persistente, opcache e invalidação entre processos ficaram de fora.
9. Também ficaram de fora API SQL/HTTP, serviços reais do Kaltura com parâmetros de classe e a geração de clientes/`KalturaTypeReflector`. O escopo do relatório já diz "no API SQL/HTTP or shared cache acceptance".
10. Papéis: o executor e o revisor deste caso foram ambos Claude CLI, então esta revisão não conta como validação independente da minha própria execução. Pelo AGENTS.md, a rotação de revisores entre Grok (ou o fallback OpenCode) e Cursor ainda precisa acontecer.
11. Não criei, editei nem reverti nenhum arquivo além dos quatro `claude-74.*`.
12. Isto não aprova a aplicação inteira: `application_acceptance` continua `false`, e o patch segue retido até os gates de release e cutover.
